[package]
name = "bevysnake"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["mp3"] }
bevy_embedded_assets = "0.15.0"
rand = "0.10.1"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
